namespace TaskTracker.Domain.Enums;

/// <summary>
/// Status category for workflow states
/// </summary>
public enum StatusCategory
{
    ToDo = 0,
    InProgress = 1,
    Done = 2,
    Cancelled = 3
}
